[Unit]
Description=Uptime Recorder
After=network.target

[Service]
Type=oneshot
ExecStart=/bin/echo "Well, this message is a Easter egg."
ExecStop=/bin/bash /usr/bin/uptimerecorder /var/log
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target
